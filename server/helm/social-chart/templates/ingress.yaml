apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: social
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  ingressClassName: nginx
  rules:
  - host: social.local
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: social
            port:
              number: 80
